import { Accessibility } from '../../types';
/**
 * @specification
 * Triggers 'expandSiblings' action with '*' on 'root'.
 * Adds role 'group' to 'root' slot.
 */
declare const hierarchicalSubtreeBehavior: Accessibility;
export default hierarchicalSubtreeBehavior;
