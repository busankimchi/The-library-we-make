/**
 * @description
 * Implements ARIA Menu Radio Group design pattern.
 * @specification
 *  Adds role='group'.
 */
var toolbarRadioGroupBehavior = function toolbarRadioGroupBehavior() {
  return {
    attributes: {
      root: {
        role: 'group'
      }
    }
  };
};

export default toolbarRadioGroupBehavior;