/**
 * @description
 * The 'list' role is used to identify an element that creates a list.
 *
 * @specification
 * Adds role='list'.
 */
var basicListBehavior = function basicListBehavior(props) {
  return {
    attributes: {
      root: {
        role: 'list'
      }
    }
  };
};

export default basicListBehavior;