/**
 * @specification
 * Adds attribute 'aria-disabled=true' based on the property 'disabled'.
 */
var textAreaBehavior = function textAreaBehavior(props) {
  return {
    attributes: {
      root: {
        'aria-disabled': props.disabled
      }
    }
  };
};

export default textAreaBehavior;