import { Accessibility } from '../../types';
/**
 * @specification
 * Adds attribute 'aria-disabled=true' based on the property 'disabled'.
 */
declare const textAreaBehavior: Accessibility<TextAreaBehaviorProps>;
export default textAreaBehavior;
declare type TextAreaBehaviorProps = {
    disabled?: boolean;
};
