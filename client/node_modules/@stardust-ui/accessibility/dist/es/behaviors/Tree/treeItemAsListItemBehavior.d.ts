import { Accessibility } from '../../types';
/**
 * @description
 * Adds role 'listitem' to a non-leaf item and 'none' to a leaf item.
 */
declare const treeItemAsListItemBehavior: Accessibility<TreeItemBehaviorProps>;
export declare type TreeItemBehaviorProps = {
    /** Indicates whether `TreeTitle` has a subtree. */
    hasSubtree?: boolean;
};
export default treeItemAsListItemBehavior;
