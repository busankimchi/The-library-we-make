import * as React from 'react';
declare type LogInspectorProps = {
    /** Will be hidden if is empty. */
    silent?: boolean;
};
declare const LogInspector: React.FunctionComponent<LogInspectorProps>;
export default LogInspector;
