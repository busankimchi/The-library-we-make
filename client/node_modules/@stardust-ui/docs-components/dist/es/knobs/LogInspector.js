import * as React from 'react';
import { KnobContext, LogContext } from './KnobContexts';

var LogInspector = function LogInspector(props) {
  var _React$useContext = React.useContext(KnobContext),
      components = _React$useContext.components;

  var _React$useContext2 = React.useContext(LogContext),
      clearLog = _React$useContext2.clearLog,
      items = _React$useContext2.items;

  var visible = props.silent ? items.length > 0 : true;
  return visible ? React.createElement(components.LogInspector, {
    clearLog: clearLog,
    items: items
  }) : null;
};

export default LogInspector;