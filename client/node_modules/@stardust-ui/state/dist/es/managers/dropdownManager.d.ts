import { Manager, ManagerConfig } from '../types';
export declare type DropdownActions<I> = {
    clear: () => void;
    open: () => void;
    close: () => void;
    select: (item: I) => void;
    unselect: (item: I) => void;
};
export declare type DropdownState<I> = {
    multiple: boolean;
    open: boolean;
    searchQuery: string;
    items: I[];
    values: string[];
};
export declare type DropdownManager<I> = Manager<DropdownState<I>, DropdownActions<I>>;
export declare const createDropdownManager: <I>(config?: Partial<ManagerConfig<DropdownState<I>, DropdownActions<I>>>) => Manager<DropdownState<I>, DropdownActions<I>>;
