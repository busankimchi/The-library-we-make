import { getColorScheme } from '../../colors';
var toolbarMenuDividerStyles = {
  root: function root(_ref) {
    var v = _ref.variables;
    var colors = getColorScheme(v.colorScheme);
    return {
      borderTop: "1px solid ".concat(v.menuDividerBorder || colors.border),
      margin: v.menuDividerMargin,
      alignSelf: 'stretch'
    };
  }
};
export default toolbarMenuDividerStyles;