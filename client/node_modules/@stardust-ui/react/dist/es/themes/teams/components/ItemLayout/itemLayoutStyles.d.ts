import { ICSSInJSStyle } from '../../../types';
declare const itemLayoutStyles: {
    root: ({ props, variables }: {
        props: any;
        variables: any;
    }) => ICSSInJSStyle;
};
export default itemLayoutStyles;
