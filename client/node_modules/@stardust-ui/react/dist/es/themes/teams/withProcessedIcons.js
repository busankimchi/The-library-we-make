import _defineProperty from "@babel/runtime/helpers/esm/defineProperty";
import { default as svgIconsAndStyles } from './components/Icon/svg/ProcessedIcons';
import { getIcon } from './index';

var declareSvg = function declareSvg(svgIcon, exportedAs) {
  return {
    isSvg: true,
    icon: svgIcon,
    exportedAs: exportedAs
  };
};

var processedIcons = Object.keys(svgIconsAndStyles).reduce(function (accIcons, iconName) {
  var iconAndMaybeStyles = svgIconsAndStyles[iconName];
  var icon = getIcon(iconAndMaybeStyles);
  return Object.assign({}, accIcons, _defineProperty({}, iconName, declareSvg(icon, iconAndMaybeStyles.exportedAs)));
}, {});
var theme = {
  icons: Object.assign({}, processedIcons, {
    'stardust-close': processedIcons['close'],
    'stardust-arrow-up': processedIcons['triangle-up'],
    'stardust-arrow-down': processedIcons['triangle-down'],
    'stardust-arrow-end': processedIcons['triangle-right']
  })
};
export default theme;