import { ICSSInJSStyle } from '../../../types';
declare const toolbarMenuStyles: {
    root: ({ props: p, variables: v }: {
        props: any;
        variables: any;
    }) => ICSSInJSStyle;
};
export default toolbarMenuStyles;
