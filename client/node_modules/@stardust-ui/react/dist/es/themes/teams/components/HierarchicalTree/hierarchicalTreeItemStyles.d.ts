import { ICSSInJSStyle } from '../../../types';
declare const hierarchicalTreeItemStyles: {
    root: ({ theme: { siteVariables } }: {
        theme: {
            siteVariables: any;
        };
    }) => ICSSInJSStyle;
};
export default hierarchicalTreeItemStyles;
