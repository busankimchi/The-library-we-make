import { pxToRem } from '../../../../lib';
var hierarchicalTreeStyles = {
  root: function root() {
    return {
      display: 'block',
      paddingLeft: "".concat(pxToRem(10))
    };
  }
};
export default hierarchicalTreeStyles;