import { ICSSInJSStyle } from '../../../types';
declare const treeTitleStyles: {
    root: ({ variables: v, theme: { siteVariables } }: {
        variables: any;
        theme: {
            siteVariables: any;
        };
    }) => ICSSInJSStyle;
};
export default treeTitleStyles;
