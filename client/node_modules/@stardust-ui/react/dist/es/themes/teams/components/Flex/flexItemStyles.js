import { toFlexAlignment, toFlexItemSizeValues } from './utils';
var flexItemStyles = {
  root: function root(_ref) {
    var p = _ref.props,
        v = _ref.variables;
    return Object.assign({}, p.align && {
      alignSelf: toFlexAlignment(p.align)
    }, p.size && toFlexItemSizeValues(v[p.size]), typeof p.shrink === 'number' && {
      flexShrink: p.shrink
    }, p.shrink === false && {
      flexShrink: 0
    }, p.grow && {
      flexGrow: p.grow
    }, p.grow === true && {
      flexGrow: 1
    }, p.itemStyles, p.push && (p.flexDirection === 'column' ? {
      marginTop: 'auto'
    } : {
      marginLeft: 'auto'
    }));
  }
};
export default flexItemStyles;