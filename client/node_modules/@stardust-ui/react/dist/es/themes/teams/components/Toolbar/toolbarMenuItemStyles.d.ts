import { ICSSInJSStyle } from '../../../types';
declare const toolbarMenuItemStyles: {
    root: ({ props: p, variables: v, theme: { siteVariables } }: {
        props: any;
        variables: any;
        theme: {
            siteVariables: any;
        };
    }) => ICSSInJSStyle;
    activeIndicator: ({ variables: v }: {
        variables: any;
    }) => ICSSInJSStyle;
    submenuIndicator: ({ variables: v }: {
        variables: any;
    }) => ICSSInJSStyle;
    wrapper: () => {
        display: string;
    };
};
export default toolbarMenuItemStyles;
