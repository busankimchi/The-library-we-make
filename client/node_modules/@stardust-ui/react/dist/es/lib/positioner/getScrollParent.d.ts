/**
 * Returns the first scrollable parent of the given element.
 * @argument {Node} node - DOM node.
 * @returns {Node} - the first scrollable parent.
 */
declare const getScrollParent: (node: Node) => Node;
export default getScrollParent;
