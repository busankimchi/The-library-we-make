/**
 * Determines if a click's coordinates are within the bounds of a node.
 *
 * @see https://github.com/Semantic-Org/Semantic-UI-React/pull/2384
 *
 * @param {object} node A DOM node.
 * @param {Event} e A SyntheticEvent or DOM Event.
 * @param {Document} target A target document.
 * @returns {boolean}
 */
declare const doesNodeContainClick: (node: HTMLElement, e: MouseEvent, target?: Document) => boolean;
export default doesNodeContainClick;
