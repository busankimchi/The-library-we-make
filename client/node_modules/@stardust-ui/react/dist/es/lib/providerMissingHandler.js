import constants from './constants';
var warning = {
  message: "You probably forgot to use a 'Provider'. Check ".concat(constants.quickStartUrl, " for correct usage."),
  wasLogged: false
};

var logProviderMissingWarning = function logProviderMissingWarning() {
  if (!warning.wasLogged) {
    /* eslint-disable-next-line no-console */
    console.error(warning.message);
    warning.wasLogged = true;
  }
};

export default logProviderMissingWarning;