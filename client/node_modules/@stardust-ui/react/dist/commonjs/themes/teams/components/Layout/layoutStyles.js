"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _defineProperty2 = _interopRequireDefault(require("@babel/runtime/helpers/defineProperty"));

var _debugStyles = require("../../../../styles/debugStyles");

var layoutStyles = {
  root: function root(_ref) {
    var props = _ref.props;
    var alignItems = props.alignItems,
        debug = props.debug,
        gap = props.gap,
        justifyItems = props.justifyItems,
        main = props.main,
        mainSize = props.mainSize,
        end = props.end,
        endSize = props.endSize,
        rootCSS = props.rootCSS,
        start = props.start,
        startSize = props.startSize,
        vertical = props.vertical;
    return Object.assign({}, debug && (0, _debugStyles.debugRoot)(), (0, _defineProperty2.default)({
      justifyItems: justifyItems,
      alignItems: alignItems,
      display: 'grid'
    }, vertical ? 'gridTemplateRows' : 'gridTemplateColumns', [// Heads up!
    // IE11 Doesn't support grid-gap, insert virtual columns instead
    start && startSize, gap && start && main && gap, main && mainSize, gap && (start || main) && end && gap, end && endSize].filter(Boolean).join(' ')), vertical && {
      gridAutoFlow: 'row'
    }, rootCSS);
  },
  gap: function gap(_ref2) {
    var props = _ref2.props;
    return Object.assign({}, props.debug && (0, _debugStyles.debugGap)({
      vertical: props.vertical
    }));
  },
  start: function start(_ref3) {
    var p = _ref3.props;
    return Object.assign({}, p.debug && (0, _debugStyles.debugArea)(), {
      alignItems: 'center',
      display: 'inline-flex'
    }, p.startCSS);
  },
  main: function main(_ref4) {
    var p = _ref4.props;
    return Object.assign({}, p.debug && (0, _debugStyles.debugArea)(), {
      alignItems: 'center',
      display: 'grid'
    }, p.mainCSS);
  },
  end: function end(_ref5) {
    var p = _ref5.props;
    return Object.assign({}, p.debug && (0, _debugStyles.debugArea)(), {
      alignItems: 'center',
      display: 'inline-flex'
    }, p.endCSS);
  }
};
var _default = layoutStyles;
exports.default = _default;